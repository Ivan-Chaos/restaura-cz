# Tasks: PDF Menu & Table Sticker Downloads

**Input**: Design documents from `/specs/007-pdf-menu-stickers/`

**Prerequisites**: plan.md, spec.md, research.md (R1–R12), data-model.md, contracts/http-api-delta.md, contracts/print-ui.md, quickstart.md

**Tests**: INCLUDED — the root, frontend and API constitutions make acceptance-scenario tests, two-sided contract tests, real-Postgres integration tests and the lint/message/token gates mandatory before merge.

**Organization**: Tasks are grouped by user story so each story is an independently implementable, testable increment.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story the task belongs to (US1–US4)
- Every task names exact file paths

## Path Conventions

Monorepo per plan.md: API work under `apps/api/`, frontend work under `apps/frontend/`. API rules: strict TS, constraints live in Postgres, migrations generated by drizzle-kit, integration tests run against real Postgres. Frontend rules: strict TS, all text via next-intl (cs+en+de in the same change), tokens only (no literal colours or arbitrary utilities in `components/` or `app/`; the QR path is `currentColor`), navigation from `@/i18n/navigation`, shadcn primitives only via the CLI, every new component gets a colocated `*.stories.tsx` and Server Components stay Server Components. The browser never calls the API; it calls only our own Route Handlers under `/api/print`. Documents render the light appearance only; the renderer is server-only (`playwright-core`, `qrcode` never reach a client bundle).

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Dependencies, configuration and environment every later task relies on. Nothing user-visible changes.

- [X] T001 Add dependencies to `apps/frontend/package.json`: runtime `playwright-core` pinned **exactly** to the installed `playwright` version (`1.62.1`, no caret, so the browser revision agrees) and `qrcode`; dev `@types/qrcode`, `jsqr`, `pdfjs-dist`; run `pnpm install` at the repo root and confirm `pnpm-lock.yaml` reuses the existing `playwright-core@1.62.1` entry
- [X] T002 [P] Update `apps/frontend/next.config.ts`: add `serverExternalPackages: ["playwright-core"]` with a doc comment explaining it is Node-only and launches a browser, so it must not be bundled (verified in `node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/serverExternalPackages.md`)
- [X] T003 [P] Document the renderer variables in `apps/frontend/.env.example` per contracts/print-ui.md §8: `PDF_RENDER_ORIGIN` (default `http://localhost:3000`, the address the headless browser uses to reach this server), `PDF_CHROMIUM_PATH` (unset → Playwright's installed Chromium), `PDF_CHROMIUM_NO_SANDBOX` (`false`; only where a container forbids the sandbox), `PDF_MAX_CONCURRENT_RENDERS` (`2`); state that the frontend must run as a Node server with a Chromium present (`pnpm exec playwright install chromium --with-deps` in the image)
- [ ] T004 [P] Verify a Chromium is available locally with `pnpm --filter frontend exec playwright install chromium` and record the command in `apps/frontend/README.md` under a new "Printing (PDF downloads)" heading together with the four env variables

**Checkpoint**: `pnpm install` clean; `pnpm --filter frontend typecheck` green; a Chromium binary exists under Playwright's cache.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The account plan (API + contract), the frontend's entitlement and validation helpers, the two QR tokens, the print CSS foundations, the `MenuSections` extraction, the renderer module, the print layout, the `Print` message namespace and the test helpers. After this phase no document can be downloaded yet, but every building block exists and is tested.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

### API: account plan

- [X] T005 Create `apps/api/src/auth/plans.ts`: `export const PLAN_IDS = ['free', 'pro', 'proPlus'] as const; export type PlanId = (typeof PLAN_IDS)[number]; export const DEFAULT_PLAN: PlanId = 'free';` with a doc comment mirroring `visual-variants.ts` (the frontend pins the same literal in `apps/frontend/lib/landing/plans.ts` / `tests/unit/plans.test.ts`; contract in `specs/007-pdf-menu-stickers/contracts/http-api-delta.md`)
- [ ] T006 [P] Create `apps/api/src/auth/plans.spec.ts`: asserts the literal equals `['free','pro','proPlus']` in that order and `DEFAULT_PLAN === 'free'`
- [X] T007 Extend `apps/api/src/db/schema.ts`: on `ownerAccount` add `plan: text('plan').notNull().default('free')` and a `check('owner_account_plan_known', sql\`${table.plan} in ('free', 'pro', 'proPlus')\`)` in the table's constraint array; doc comment: "the pinned vocabulary from `auth/plans.ts`; no endpoint writes it yet — billing will" (depends on T005)
- [X] T008 Generate the migration with `pnpm --filter api db:generate` → `apps/api/src/db/migrations/0005_*.sql` + `meta/0005_snapshot.json` + `_journal.json`; review that the SQL adds one `NOT NULL DEFAULT 'free'` column and one CHECK and nothing else; apply with `pnpm --filter api db:migrate` against the dev database (depends on T007)
- [X] T009 Extend `apps/api/src/auth/auth.service.ts`: add `plan: PlanId` to `PublicAccount`; sign-up returns `{ ...row, emailVerified: false, plan: DEFAULT_PLAN }` (also select `plan` in `.returning`); every other select that builds a `PublicAccount` (sign-in, `resolveSession` around line 490, the resend-confirmation lookup if it builds one) reads `plan: ownerAccount.plan`; import `PlanId`/`DEFAULT_PLAN` from `./plans.js` (depends on T005, T007)
- [X] T010 [P] Extend `apps/api/test/helpers.ts`: `export async function setPlan(accountId: string, plan: string): Promise<void>` updating `owner_account.plan` through the same client `markEmailVerified` uses (accept `string` so a test can send an unknown plan and expect the CHECK to reject it)
- [X] T011 Extend `apps/api/test/auth.e2e-spec.ts`: sign-up, sign-in and `GET /auth/me` account bodies now equal `{ id, email, emailVerified, plan: 'free' }`; new cases "reports the account's plan after it changes" (`setPlan(id, 'pro')` → `/auth/me` has `plan: 'pro'`) and "the database refuses an unknown plan" (`setPlan(id, 'gold')` rejects with a check-violation error); run `RESEND_API_KEY= pnpm --filter api test:e2e` (depends on T009, T010)
- [X] T012 [P] Mirror contracts/http-api-delta.md into `specs/001-menu-creation-publishing/contracts/http-api.md`: add a "Plan ids (feature 007)" block after "Image references (feature 006)", add `plan` to every documented account body, add the contract-test obligations line

### Frontend: types, entitlement, validation

- [X] T013 Extend `apps/frontend/lib/api/types.ts`: import `type { PlanId } from "@/lib/landing/plans"`; add `plan: PlanId` to `Account` with a doc comment (defaults to `free`; consumers must treat a missing value as `free`; drives the branding entitlement in feature 007)
- [X] T014 [P] Create `apps/frontend/lib/plans/entitlements.ts`: `planOf(account: { plan?: unknown }): PlanId` (returns the value when it is one of `PLANS.map(p => p.id)`, else `"free"`); `canRemoveBranding(plan: PlanId): boolean` (`plan !== "free"`); `resolveBranding(plan: PlanId, requested: boolean | undefined): boolean` per the data-model table (Free → `true`; paid → `requested ?? false`); doc comments cite spec FR-015..FR-017
- [X] T015 [P] Create `apps/frontend/lib/validation/print.ts`: `STICKER_COUNT_MIN = 1`, `STICKER_COUNT_MAX = 200`, `stickerCountSchema = z.coerce.number().int().min(1).max(200)`, `brandingParamSchema` (`"0" | "1"` → boolean, anything else → `undefined`), `printLocaleSchema` (`z.enum(routing.locales)` default `"cs"`), `parseStickerOptions(params: URLSearchParams)` and `parseMenuOptions(params)` returning `{ ok: true, value } | { ok: false }`; export types `PrintOptions`, `StickerOptions`
- [ ] T016 [P] Create `apps/frontend/tests/unit/plans.test.ts`: literal pin `["free","pro","proPlus"]` equals `PLANS.map(p => p.id)` (copied, not imported, from `apps/api/src/auth/plans.ts`, as `variants.test.ts` does); `planOf` falls back to `free` for `undefined`, `null`, `"gold"`; `canRemoveBranding` table; `resolveBranding` full table from data-model.md (depends on T014)
- [ ] T017 [P] Create `apps/frontend/tests/unit/print-validation.test.ts`: `stickerCountSchema` accepts `"1"`, `"200"`, `12`; rejects `0`, `201`, `2.5`, `""`, `"abc"`; `parseStickerOptions` and `parseMenuOptions` on representative `URLSearchParams` including an invalid `locale` (depends on T015)
- [X] T018 Extend `apps/frontend/tests/unit/api-contract.test.ts`: every account fixture carries `plan: "free"`; add a case that `AccountResponse` accepts each of `"free"`, `"pro"`, `"proPlus"` and (type-level) rejects `"gold"` (depends on T013)

### Frontend: design tokens and print CSS (foundations)

- [X] T019 Extend `apps/frontend/lib/design-system/tokens.ts`: add `"qr-foreground"` and `"qr-surface"` to `MENU_COLOR_TOKENS`; add `TOKEN_PURPOSE` entries ("Modules of a machine-readable code (QR). Always a near-black step, in both appearances — inverted codes fail on many scanners." / "The tile a QR code sits on. Always a near-white step, in both appearances."); add `{ foreground: "qr-foreground", background: "qr-surface", min: 7 }` to `CONTRAST_PAIRS` with a comment citing spec FR-011
- [X] T020 Declare `--qr-foreground` and `--qr-surface` in **both** the light and the dark blocks of every theme file — `apps/frontend/styles/themes/warm.css` (cocoa-950 on cream-50, in the shared `:root, [data-theme="warm"], [data-appearance="light"]` rule and the `.dark` rule), `plain-white.css`, `liquid-glass.css`, `modern.css` (ink-950 on ink-50), `green-bar.css` (bottle-950 on cream-50), `refined.css` (cocoa-900/950 on ivory-50), `slate.css` (graphite-950 on graphite-50) — using only palette steps that exist in `styles/palette.css`; run `pnpm --filter frontend test:unit` and confirm `themes.test.ts` (required tokens, catalogue-only, palette steps exist) and `contrast.test.ts` (7:1 in every theme × appearance) pass (depends on T019)
- [X] T021 [P] Extend `apps/frontend/app/globals.css`: add `@media print { [data-theme] { --panel: var(--surface-raised); --panel-blur: 0px; --ambient: none; --ambient-motion: none; } }` beside the reduced-transparency block, with a comment: paper has no blur and no drift (spec FR-003); `theme-css.ts`'s flat parser never reads `globals.css`, so this is the sanctioned place
- [X] T022 [P] Create `apps/frontend/app/[locale]/print/print.css`: `@page { size: A4; margin: 12mm 14mm; }`; `.print-running-band` fixed top band height (`10mm`) and `body`/document top padding to match; `.print-page` (sticker page) `width: 182mm; height: 273mm` (A4 minus margins), `break-after: page`, `display: grid; grid-template: 1fr 1fr / 1fr 1fr`; cut-guide rules on the grid's shared edges using `border-color: var(--border)` and `border-style: dashed`; `.print-avoid-break { break-inside: avoid; }`, `.print-keep-with-next { break-after: avoid; }`; no literal colours (tokens via `var(--…)` only; lengths in mm are print geometry, not a design scale — add a header comment saying so and, if `scripts/check-design-tokens.mjs` ever scans CSS, why this file is exempt)

### Frontend: shared menu body

- [X] T023 Create `apps/frontend/components/menu/MenuSections.tsx` (Server Component) per contracts/print-ui.md §4: props `{ categories: MenuCategory[]; presentation: Presentation; avoidPageBreaks?: boolean }`; move the per-category loop out of `GuestMenu.tsx` verbatim (photographed → `presentation.cards` else `presentation.rows`; classic keeps its picture rule; `Grid` of `DishCard` with surface/priceTreatment/priority, or a column of `DishRow`; `CategoryHeading` with `index`; `MenuPanel` when `presentation.panel`); when `avoidPageBreaks`, wrap each dish in `<div className="print-avoid-break">` and add `print-keep-with-next` to the heading wrapper; keep `section` ids/`aria-labelledby` identical
- [X] T024 Refactor `apps/frontend/components/menu/GuestMenu.tsx` to render `<MenuSections categories={categories} presentation={presentation} />` inside the existing `<main><Container size="md">`, keeping the header, nav, empty-menu spacer and footer exactly as they are; run `pnpm --filter frontend test:e2e -g "public menu"` and `test:stories` to prove no change (depends on T023)

### Frontend: renderer module (server-only)

- [X] T025 [P] Create `apps/frontend/lib/pdf/semaphore.ts`: `createSemaphore(limit: number)` returning `{ acquire(): Promise<() => void> }` (FIFO queue); `export const renderSemaphore = createSemaphore(readLimit())` where `readLimit()` parses `PDF_MAX_CONCURRENT_RENDERS` (positive integer, default 2, throws a readable error otherwise); unit test in `apps/frontend/tests/unit/semaphore.test.ts` (never more than `limit` concurrent, FIFO release)
- [X] T026 [P] Create `apps/frontend/lib/pdf/browser.ts`: `getBrowser(): Promise<Browser>` from `playwright-core`'s `chromium.launch({ executablePath: process.env.PDF_CHROMIUM_PATH || undefined, chromiumSandbox: process.env.PDF_CHROMIUM_NO_SANDBOX !== "true" })`, memoised on `globalThis` under a symbol so dev HMR reuses it; relaunch when `!browser.isConnected()`; export `renderOrigin(): string` (`PDF_RENDER_ORIGIN` or `http://localhost:${process.env.PORT ?? 3000}`, trailing slash stripped); module doc comment: server-only, one browser per process, a context per request
- [X] T027 Create `apps/frontend/lib/pdf/render.ts` per contracts/print-ui.md §3: `interface PrintTarget { path: string; search: URLSearchParams; timeoutMs: number }`; `renderPdf(target, session: { name: string; value: string }): Promise<Buffer>` and `renderPreview(target, session): Promise<Buffer>`; both: acquire the semaphore, `browser.newContext({ colorScheme: "light", viewport: { width: 794, height: 1123 }, deviceScaleFactor: 1 })`, `context.addCookies([{ name, value, url: origin, httpOnly: true, sameSite: "Lax" }])`, `page.emulateMedia({ media: "print", colorScheme: "light", reducedMotion: "reduce" })`, `page.goto(origin + path + "?" + search, { waitUntil: "networkidle", timeout })`, `await page.evaluate(() => document.fonts.ready)`, then `page.pdf({ format: "A4", printBackground: true, preferCSSPageSize: true, tagged: true, margin: { top: 0, right: 0, bottom: 0, left: 0 } })` or `page.screenshot({ type: "jpeg", quality: 80, clip: { x: 0, y: 0, width: 794, height: 1123 } })`; wrap in a race against `timeoutMs` throwing `RenderTimeoutError`; `finally { await context.close(); release() }`; if the print page responded with a non-200 status, throw `RenderTargetError(status)` (depends on T025, T026)
- [X] T028 [P] Create `apps/frontend/lib/pdf/filename.ts`: `slugForFilename(name: string)` (NFD, strip `\p{M}`, lowercase, `[^a-z0-9]+` → `-`, trim `-`, ≤ 60 chars, empty → `"menu"`); `documentFilename(menuName, suffix)` → `${slug}-${suffix}.pdf`; `contentDisposition(filename)` → `attachment; filename="<ascii>"; filename*=UTF-8''<encodeURIComponent>`; unit test in `apps/frontend/tests/unit/print-filename.test.ts` ("Polední menu" + "jidelni-listek" → `poledni-menu-jidelni-listek.pdf`; non-Latin name → `menu-…`; header shape)
- [X] T029 Create `apps/frontend/lib/pdf/request.ts` per contracts/print-ui.md §2: `type PrintErrorCode = "UNAUTHENTICATED" | "NOT_FOUND" | "VALIDATION_FAILED" | "NOT_PUBLISHED" | "EMPTY_MENU" | "RENDER_TIMEOUT" | "RENDER_FAILED"`; `errorResponse(code)` mapping to 401/404/400/409/409/504/500 with body `{ error: { code } }` and `Cache-Control: private, no-store`; `loadPrintContext(menuId)`: `getSession()` (null → `UNAUTHENTICATED`), `getMenu(menuId)` (not ok → `NOT_FOUND`), session cookie from `cookies()`, `plan = planOf(session.account)`; `pdfResponse(buffer, filename)` and `jpegResponse(buffer)` helpers setting `Content-Type`, `Content-Length`, disposition and `no-store`; `runRender(fn)` translating `RenderTimeoutError` → `RENDER_TIMEOUT`, anything else → `RENDER_FAILED` after `console.error` (depends on T013, T014, T015, T027, T028)

### Frontend: print layout and shared print components

- [X] T030 [P] Create `apps/frontend/app/[locale]/print/layout.tsx`: validates `hasLocale`, `setRequestLocale`, imports `./print.css`, renders `<div data-slot="print-root" className="bg-background text-foreground">{children}</div>` — no `CookieBanner`, no appearance toggle; `export const dynamic = "force-dynamic"` with the documented reason (one owner's private menu behind a session, like `/preview`); `generateMetadata` → `robots: { index: false, follow: false }`
- [X] T031 [P] Create `apps/frontend/components/print/PoweredBy.tsx` (Server Component) + `PoweredBy.stories.tsx`: `useTranslations("Print")`, renders `<p data-slot="powered-by" className="text-muted-foreground text-xs">` with `t("poweredBy")` (the visible address `restaura.cz` inside the message); story shows it in two themes
- [X] T032 [P] Create `apps/frontend/components/print/RunningHeader.tsx` (Server Component): props `{ title: string; restaurantName: string }`; renders the fixed `.print-running-band` with the menu title in `font-display text-sm` and the restaurant name in `text-muted-foreground text-xs`, `aria-hidden` (the header proper carries the accessible identity)
- [X] T033 Add the `Print` namespace to `apps/frontend/messages/en.json`, `cs.json`, `de.json` with every key from contracts/print-ui.md §7 (`downloadMenu`, `downloadStickers`, `menuNeedsDishes`, `stickersNeedPublish`, `dialogTitle.{menu,stickers}`, `dialogDescription.{menu,stickers}`, `countLabel`, `countHint`, `countInvalid` naming 1–200, `brandingLabel`, `brandingHint`, `previewAlt`, `previewLoading`, `previewUnavailable`, `download`, `preparing`, `retry`, `downloaded`, `cancel`, `errors.{UNAUTHENTICATED,NOT_FOUND,VALIDATION_FAILED,NOT_PUBLISHED,EMPTY_MENU,RENDER_TIMEOUT,RENDER_FAILED}`, `poweredBy`, `scanPrompt`, `table` with `{number}`, `fileSuffix.{menu,stickers}` (cs `jidelni-listek`/`stolky`, en `menu`/`table-stickers`, de `speisekarte`/`tischaufkleber`), `metaTitle.{menu,stickers}`); run `node scripts/check-messages.mjs` from `apps/frontend/`

### Test helpers

- [ ] T034 [P] Extend `apps/frontend/tests/e2e/helpers/database.ts`: `export async function setPlan(email: string, plan: "free" | "pro" | "proPlus"): Promise<void>` updating `owner_account.plan` by `lower(email)`, in the style of `removeProfile`
- [ ] T035 [P] Extend `apps/frontend/tests/e2e/helpers/owner.ts`: `openDownloadDialog(page, kind: "menu" | "stickers")` (clicks the trigger by its `Print.downloadMenu`/`downloadStickers` label from the message catalogues), `downloadDocument(page, kind, options?: { count?: number; branding?: boolean })` returning the Playwright `Download` (fills count, toggles the switch when present, clicks Download, awaits `page.waitForEvent("download")`), `readPdf(download)` using `pdfjs-dist` to return `{ pages: number; text: string[] }` (text per page), `decodeQr(page, locator)` screenshotting the tile and decoding with `jsqr` via `sharp` (dev dep) to raw RGBA
- [ ] T036 [P] Extend `apps/frontend/tests/e2e/helpers/owner.ts` with `addManyItems(page, section, count)` (or a database-seeded fast path in `helpers/database.ts`, `seedItems(menuSlugOrId, count)`) so the budget tests can build a 200-dish menu in seconds rather than by clicking 200 forms

**Checkpoint**: `pnpm --filter api test && RESEND_API_KEY= pnpm --filter api test:e2e` green with `plan` on every account; `pnpm --filter frontend lint && typecheck && test:unit && test:stories` green (new tokens in every theme, `MenuSections` extracted with no visible change, `Print` catalogue complete in three locales); the renderer module compiles but nothing calls it yet.

---

## Phase 3: User Story 1 - Owner Downloads a Printable Menu (Priority: P1) 🎯 MVP

**Goal**: From the editor, an owner downloads their menu (draft or published) as an A4 PDF in the menu's visual style: identity, sections, every dish with description and price, photos where present, the menu name on every page, no dish split across pages, and the branding line on the final page. Free accounts see no branding option.

**Independent Test**: Create a menu with three sections, a mix of dishes with and without photos, choose Refined, click "Download menu PDF", open the file: everything present, recognisably Refined, "Powered by restaura.cz" at the end. Switch to Green Bar and download again: visibly different, same content. An empty menu shows the button disabled.

### Print composition and page

- [X] T037 [P] [US1] Create `apps/frontend/components/print/PrintMenu.tsx` (Server Component) per contracts/print-ui.md §4: props `{ menu: Menu; presentation: Presentation; showBranding: boolean; runningTitle: string; restaurantName: string }`; renders `RunningHeader` → `MenuHeader establishment layout={presentation.header}` (no `actions`) → `<main><Container size="md"><MenuSections avoidPageBreaks … /></Container></main>` → `<Container size="md"><MenuFooter … /></Container>` → `PoweredBy` when `showBranding` inside a `print-avoid-break` footer block
- [ ] T038 [P] [US1] Create `apps/frontend/components/print/PrintMenu.stories.tsx`: stories for every presentation recipe using `SAMPLE_MENU` from `lib/design-system/fixtures/sample-menu.ts` (with and without branding), each wrapped in the theme's `ThemeScope`; an "Empty section" story; the story parameters emulate print media where the Storybook setup allows (`parameters: { chromatic: … }` not required); axe runs in both passes
- [X] T039 [US1] Create `apps/frontend/app/[locale]/print/menu/[menuId]/page.tsx`: `hasLocale` + `setRequestLocale`; `const { account, profile } = await requireProfile(locale)`; `getMenu(menuId)` (not ok → `notFound()`); if no section has an item → `notFound()`; parse `branding` with `brandingParamSchema` from `searchParams`; `showBranding = resolveBranding(planOf(account), requested)`; `themeId = themeForVariant(menu.visualVariant)`; render `<ThemeScope theme={themeId} className="bg-background text-foreground"><PrintMenu menu={toDisplayMenu({ ...menu, restaurantName: profile.restaurantName, logo: profile.logo })} presentation={presentationForTheme(themeId)} showBranding runningTitle={menu.name} restaurantName={profile.restaurantName} /></ThemeScope>`; `<title>` from `Print.metaTitle.menu`; `export const dynamic = "force-dynamic"` (depends on T037)

### Route handler

- [X] T040 [US1] Create `apps/frontend/app/api/print/menu/[menuId]/route.ts`: `GET(request, { params })`: `loadPrintContext(menuId)` → on error `errorResponse`; `parseMenuOptions(request.nextUrl.searchParams)` → `VALIDATION_FAILED`; empty menu → `EMPTY_MENU`; `showBranding = resolveBranding(plan, options.branding)`; `renderPdf({ path: \`/${locale}/print/menu/${menuId}\`, search: new URLSearchParams({ branding: showBranding ? "1" : "0" }), timeoutMs: 25_000 }, sessionCookie)` via `runRender`; `pdfResponse(buffer, documentFilename(menu.name, t("fileSuffix.menu")))` using `getTranslations({ locale, namespace: "Print" })` (depends on T029, T039)

### Download UI

- [X] T041 [US1] Create `apps/frontend/components/workspace/PrintDownloadDialog.tsx` (`"use client"`) per contracts/print-ui.md §4 — this task builds the **menu** path; stickers fields (T051) and preview (T063) are added later behind the same props: props `{ kind; locale; menuId; canRemoveBranding; open; onOpenChange; fetchDocument?: typeof fetch }`; shadcn `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter`; body has an options column (empty for `menu` on Free) and a preview slot (placeholder until T063); footer: `<form method="get" action={documentUrl} onSubmit={intercept}>` with a `Button type="submit"` labelled `Print.download`; JS path: `fetchDocument(documentUrl)` → `!ok` → parse `{ error: { code } }` (fallback `RENDER_FAILED`) → `role="alert"` with `t(\`errors.${code}\`)` and a Retry `Button`; ok → `blob()` → object URL → hidden `<a download={filenameFrom(response.headers.get("content-disposition"))}>` click → `toast.success(t("downloaded"))` → close; pending: submit disabled, `t("preparing")` in an `aria-live="polite"` region; `documentUrl` built per contracts/print-ui.md §5 (`branding` param from local state, `1` when not entitled)
- [X] T042 [P] [US1] Create `apps/frontend/components/workspace/PrintDownloads.tsx` (`"use client"` thin host) per contracts/print-ui.md §4: props `{ locale; menuId; menuName; status; hasDishes; plan }`; two `Button variant="outline"` triggers with lucide `FileText` and `QrCode` icons and labels `Print.downloadMenu`/`downloadStickers`; menu trigger disabled when `!hasDishes` with `Print.menuNeedsDishes` as visible helper text (`aria-describedby`); stickers trigger disabled when `status !== "published"` with `Print.stickersNeedPublish`; hosts one `PrintDownloadDialog` per kind with `canRemoveBranding={canRemoveBranding(plan)}`; mounts a `<Toaster />` only if the editor route does not already have one (check `SectionEditor`/`VariantSwitcher` usage; do not duplicate)
- [X] T043 [US1] Wire `apps/frontend/app/[locale]/workspace/menus/[menuId]/page.tsx`: read the session's plan (`getSession()` is cached; the workspace layout already fetched it) and render `<PrintDownloads locale menuId={menu.id} menuName={menu.name} status={menu.status} hasDishes={menu.sections.some(s => s.items.length > 0)} plan={planOf(session.account)} />` directly under `PublishControls` (depends on T041, T042)
- [ ] T044 [P] [US1] Create `apps/frontend/components/workspace/PrintDownloadDialog.stories.tsx`: menu kind, Free (no switch), with a stubbed `fetchDocument` returning a tiny PDF blob (success), a `{ error: { code: "RENDER_TIMEOUT" } }` 504 (error + retry), and a never-resolving promise (pending state); plus a `PrintDownloads.stories.tsx` showing enabled, empty-menu-disabled and draft-disabled states; axe in both passes

### Tests for User Story 1

- [ ] T045 [US1] Create `apps/frontend/tests/e2e/print.spec.ts` with a `describe("menu PDF")` block: (a) download from a draft menu with three sections → `Download` event, suggested filename matches `/^[a-z0-9-]+-jidelni-listek\.pdf$/`, `readPdf` text contains every section title, dish name and price, "Powered by" appears exactly once and on the last page; (b) style switch: download as `default` and as `green-bar`, assert both contain the same dish text and that the two PDFs' byte lengths or first-page raster hashes differ; (c) empty menu → menu trigger disabled and its hint visible; (d) a 60-dish menu (via T036 helper) → every page's text contains the menu name and each dish name and its price share a page; (e) ownership: sign in as owner B in a second context and `request.get("/api/print/menu/<A's id>?locale=cs")` → 404 JSON `NOT_FOUND`; signed-out → 401; (f) Free plan: no branding switch in the dialog; `request.get(url + "&branding=0")` still yields "Powered by" (depends on T035, T036, T043)
- [ ] T046 [P] [US1] Add a Liquid Glass light-appearance case to `print.spec.ts`: set the dashboard to dark via the appearance toggle on the public sample page (or `localStorage.restaura-appearance = "dark"`), download a `liquid-glass` menu, rasterise page 1 with `pdfjs-dist` and assert the sampled ground pixel is light (luminance > 0.8) — proves FR-003 (depends on T045)

**Checkpoint**: A Free owner downloads a styled menu PDF from the editor; the API, lint, typecheck, unit, stories and `print.spec.ts` "menu PDF" pass. Ship-able MVP.

---

## Phase 4: User Story 2 - Owner Downloads Table QR Stickers (Priority: P1)

**Goal**: For a published menu, the owner enters a sticker count (1–200) and downloads an A4 sheet with four numbered stickers per page, each with a scannable QR code (public address + `?table=N`), the restaurant identity, a localized scan prompt, cut guides and the branding line.

**Independent Test**: Publish a menu, request 10 stickers, download: three pages (4 + 4 + 2), numbers 1–10, print page one at 100 % and scan each code with a phone: the menu opens. Invalid counts are rejected inline. A draft menu shows the button disabled.

### QR and sticker components

- [X] T047 [P] [US2] Create `apps/frontend/lib/pdf/qr.ts`: `qrModules(text: string): { size: number; path: string }` using `qrcode`'s `create(text, { errorCorrectionLevel: "Q" })` → `modules.size` and `modules.data`, building one SVG path of `M${x} ${y}h1v1h-1z` squares for dark modules; `QUIET_ZONE = 4`; unit test in `apps/frontend/tests/unit/qr.test.ts`: a known input yields a square matrix of version-appropriate size, the path is deterministic, and (round-trip) rasterising the path with `sharp` and decoding with `jsqr` returns the input text
- [X] T048 [P] [US2] Create `apps/frontend/lib/pdf/sticker-url.ts`: `publicMenuUrl(locale: Locale, slug: string, table: number): string` = `${siteOrigin()}/${locale}/m/${slug}?table=${table}` where `siteOrigin()` is `NEXT_PUBLIC_SITE_URL` (default `http://localhost:3000`) without trailing slash; extend `tests/unit/print-validation.test.ts` with the exact string for a sample
- [X] T049 [P] [US2] Create `apps/frontend/components/print/QrCode.tsx` (Server Component) + `QrCode.stories.tsx`: props `{ value: string; label: string; className? }`; renders a `div` tile `bg-qr-surface text-qr-foreground rounded-md p-3` containing `<svg role="img" aria-labelledby={id} viewBox={\`0 0 ${size} ${size}\`} shape-rendering="crispEdges" className="size-full"><title id={id}>{label}</title><path fill="currentColor" d={path} /></svg>`; the padding is the quiet zone (≥ 4 modules at the rendered size — compute padding as a percentage of the tile: `p-[…]` is forbidden, so use the tile's own `aspect-square` box with `p-3` and a comment showing the module maths at 44 mm); stories in warm and green-bar to show the tile stays dark-on-light (depends on T047)
- [X] T050 [P] [US2] Create `apps/frontend/components/print/Sticker.tsx` (Server Component): props `{ tableNumber; url; restaurantName; logo?: ImageModel; prompt; tableLabel; showBranding }`; a fixed-height cell (`h-full`) laid out as: identity row (`SafeImage` logo `size-10 rounded-md` with `fallback` of the name in `font-display`, or the name alone), `prompt` in `text-muted-foreground text-sm`, `QrCode` in a `w-3/5 mx-auto` box (≈ 44 mm on a quarter-A4 cell), `tableLabel` in `font-display text-5xl leading-none tabular-nums`, `PoweredBy` when `showBranding`; `break-words` on the name; nothing may overflow the cell
- [X] T051 [US2] Create `apps/frontend/components/print/StickerSheet.tsx` (Server Component) + `StickerSheet.stories.tsx`: props `{ count; restaurantName; logo?; slug; locale; prompt; tableLabel: (n: number) => string; showBranding }`; renders `Math.ceil(count / 4)` `<section className="print-page">` each with up to four `Sticker`s (`url = publicMenuUrl(locale, slug, n)`), cut guides from `print.css` on shared edges only, empty cells on the last page unguided; stories: 1, 4, 10 stickers, with/without logo, with/without branding, in warm/green-bar/refined; axe both passes (depends on T048, T049, T050)

### Print page and route handler

- [X] T052 [US2] Create `apps/frontend/app/[locale]/print/stickers/[menuId]/page.tsx`: same gate and menu fetch as the menu page; `menu.status !== "published" || !menu.publicSlug` → `notFound()`; `parseStickerOptions(searchParams)` not ok → `notFound()`; `showBranding` as before; `t = await getTranslations({ locale, namespace: "Print" })`; render `<ThemeScope …><StickerSheet count restaurantName={profile.restaurantName} logo={toImageModel(profile.logo, profile.restaurantName)} slug={menu.publicSlug} locale prompt={t("scanPrompt")} tableLabel={(n) => t("table", { number: n })} showBranding /></ThemeScope>` (depends on T051)
- [X] T053 [US2] Create `apps/frontend/app/api/print/stickers/[menuId]/route.ts`: as the menu handler but `parseStickerOptions` (→ `VALIDATION_FAILED`), `status !== "published"` → `NOT_PUBLISHED`, `search = { count, branding }`, `timeoutMs: 15_000`, filename suffix `fileSuffix.stickers` (depends on T029, T052)

### Dialog: count field

- [X] T054 [US2] Extend `apps/frontend/components/workspace/PrintDownloadDialog.tsx` for `kind === "stickers"`: a shadcn `Field` with `Label` `Print.countLabel`, `Input type="number" inputMode="numeric" min=1 max=200 step=1` default `12`, helper `Print.countHint`, validated on change with `stickerCountSchema`; invalid → field error `Print.countInvalid` (names 1–200) and the Download button disabled; `count` appended to `documentUrl` (depends on T041, T053)
- [ ] T055 [P] [US2] Extend `apps/frontend/components/workspace/PrintDownloadDialog.stories.tsx`: stickers kind with valid, invalid (`0`, `201`, `2.5`) and blank counts; axe both passes (depends on T054)

### Tests for User Story 2

- [ ] T056 [US2] Extend `apps/frontend/tests/e2e/print.spec.ts` with `describe("table stickers")`: (a) draft → stickers trigger disabled with hint; after `publish(page)` enabled; (b) count 10 → 3 pages; text contains "Stůl 1" … "Stůl 10" once each, the restaurant name and "Naskenujte…" on every page, "Powered by" 10 times; (c) count 4 → 1 page, count 1 → 1 page; (d) invalid counts `0`, `201`, `2.5`, blank → inline message containing "1" and "200", Download disabled, no request to `/api/print` (assert via `page.on("request")`); (e) handler: `request.get(".../stickers/<id>?locale=cs&count=0")` → 400 `VALIDATION_FAILED`; draft menu → 409 `NOT_PUBLISHED`; (f) suggested filename ends in `-stolky.pdf`; (g) unpublish → republish → the sticker URL from (b) still resolves 200 as a guest (depends on T035, T054)
- [ ] T057 [P] [US2] Add a QR decode case to `print.spec.ts`: open `/cs/print/stickers/<id>?count=4` directly as the owner, for each `[data-slot="qr-code"]` tile call `decodeQr` and expect `${baseURL}/cs/m/${slug}?table=${n}` for n = 1..4; also assert the tile's computed background luminance is light and the path colour dark in `green-bar` (SC-003, FR-011) (depends on T035, T052)
- [ ] T058 [P] [US2] Extend `apps/frontend/tests/e2e/public-menu.spec.ts`: the guest page's `main` text content for `/cs/m/<slug>` equals that of `/cs/m/<slug>?table=3` and both return 200; a draft menu with `?table=3` still 404s (SC-008, FR-010)

**Checkpoint**: Stickers download for published menus, QR codes decode to the public address with `?table=N`, invalid counts never leave the dialog, the guest page is untouched. US1 and US2 both work independently.

---

## Phase 5: User Story 3 - Paid-Plan Owner Removes Restaura Branding (Priority: P2)

**Goal**: Pro and Pro Plus owners get a "Show Restaura branding" switch (default off) in both dialogs; documents honour it; Free owners never see it and cannot bypass it; the subscription tab shows the real plan.

**Independent Test**: With an account set to `pro` in the database, both dialogs show the switch defaulting to off; downloads carry no branding line and the space is closed up; switching it on brings the line back. Set `free`: the switch is gone and every document carries the line.

- [X] T059 [US3] Extend `apps/frontend/components/workspace/PrintDownloadDialog.tsx`: when `canRemoveBranding`, render a shadcn `Switch` with `Label` `Print.brandingLabel` and helper `Print.brandingHint`, default `false`; `branding` query param reflects the switch (`1`/`0`); when not entitled, no switch is rendered and the param is always `1` (depends on T041)
- [ ] T060 [P] [US3] Extend `PrintDownloadDialog.stories.tsx`: Pro variants of both kinds with the switch off and on; axe both passes (depends on T059)
- [X] T061 [P] [US3] Update `apps/frontend/app/[locale]/workspace/settings/subscription/page.tsx`: read `getSession()`, `plan = planOf(session.account)`, render `tPlans(\`${plan}.name\`)` from the `Landing.plans` namespace as the card title; update `Settings.subscriptionTab.planDescription`/`comingSoon` copy in `messages/{cs,en,de}.json` so it no longer claims one plan for everyone (e.g. "Your current plan." / "Plan changes and billing will be managed here."); adjust `tests/e2e/settings.spec.ts` if it asserts the old constant
- [ ] T062 [US3] Extend `apps/frontend/tests/e2e/print.spec.ts` with `describe("branding by plan")`: (a) `setPlan(email, "pro")` → reload editor → both dialogs show the switch, unchecked; menu PDF text lacks "Powered by"; stickers text lacks it; the last page's text ends with the footer content (no trailing blank strip is a visual check — assert page count equals the Free download's page count for the same menu); (b) switch on → "Powered by" present; (c) `setPlan(email, "proPlus")` behaves like Pro; (d) back to `free` → no switch; `request.get(url + "&branding=0")` → "Powered by" present (FR-017); (e) settings → subscription shows "Pro" for a pro account (depends on T034, T059, T061)

**Checkpoint**: Entitlement is visible, honoured and tamper-proof; all three stories work independently.

---

## Phase 6: User Story 4 - Owner Previews Before Downloading (Priority: P3)

**Goal**: Each dialog shows the first page of the document as it will be produced, updating when the count or branding changes, and degrades to a notice (download still available) when the preview cannot be produced.

**Independent Test**: Open either dialog: a first-page image appears within 3 s in the menu's style; change the sticker count to 3 or toggle branding: the image refreshes within 2 s; block the preview endpoint: a notice appears and Download still works.

- [X] T063 [P] [US4] Create `apps/frontend/app/api/print/menu/[menuId]/preview/route.ts`: as the menu PDF handler but `renderPreview(…, timeoutMs: 10_000)` and `jpegResponse(buffer)`; same error codes (depends on T040)
- [X] T064 [P] [US4] Create `apps/frontend/app/api/print/stickers/[menuId]/preview/route.ts`: as the stickers PDF handler but `renderPreview` and `jpegResponse` (depends on T053)
- [X] T065 [US4] Extend `apps/frontend/components/workspace/PrintDownloadDialog.tsx`: preview slot renders `<img alt={t("previewAlt")} src={previewUrl} width={794} height={1123} className="border-border h-auto w-full rounded-md border" />` — the explicit intrinsic dimensions give the box its A4 proportion before the bytes arrive (no arbitrary `aspect-[…]` utility, which the token gate forbids); `previewUrl` recomputed from the current options after a 400 ms debounce (`useEffect` + `setTimeout`) and only while the count is valid; `Skeleton` overlay while loading (`onLoad` clears it) with `t("previewLoading")` for assistive tech; `onError` → replace the image with a `Print.previewUnavailable` notice (`role="status"`) and keep Download enabled; the image is `tabIndex={-1}` and `draggable={false}` (depends on T054, T059, T063, T064)
- [ ] T066 [P] [US4] Extend `PrintDownloadDialog.stories.tsx`: preview loading, loaded (a static sample JPEG data URI passed through a `previewSrc` override prop used only by stories), and unavailable states (depends on T065)
- [ ] T067 [US4] Extend `apps/frontend/tests/e2e/print.spec.ts` with `describe("preview")`: (a) open the menu dialog → the preview `<img>` fires `load` within 3 s and its natural size is 794 × 1123; (b) stickers: change count 12 → 3 → `src` changes and reloads within 2 s; toggle branding (pro) → `src` changes; (c) `page.route("**/api/print/**/preview**", r => r.fulfill({ status: 500, body: JSON.stringify({ error: { code: "RENDER_FAILED" } }) }))` → notice visible, Download still produces a file; (d) keyboard-only: Tab to the trigger, Enter, Tab through count/switch/Download, Enter → download event; axe on the open dialog via `@axe-core/playwright` (depends on T065)

**Checkpoint**: All four stories complete and independently verifiable.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Budgets, documentation, resilience checks and the final gates.

- [ ] T068 [P] Add budget cases to `apps/frontend/tests/e2e/print.spec.ts` (`describe("budgets")`, tagged `@slow`): a 200-dish menu with ≥ 20 photographed dishes (seed via T036 + `uploadImage` helper) downloads in ≤ 10 s and ≤ 15 MB (PR-001, PR-003); the same menu without photos ≤ 2 MB; 200 stickers in ≤ 10 s and ≤ 5 MB (PR-002); first preview ≤ 3 s (PR-004); measure with `performance.now()` around `downloadDocument`
- [ ] T069 [P] Add a resilience case to `print.spec.ts`: fire six preview requests concurrently via `request.get` → all resolve 200 (the semaphore queues, nothing 5xx); and a renderer-failure case using a dedicated `test.describe.serial` that sets `PDF_RENDER_ORIGIN=http://127.0.0.1:9` for a second frontend server if `playwright.config.ts` can start one cheaply, otherwise document in quickstart.md that FR-023 is covered by the unit test of `runRender` mapping errors to `RENDER_FAILED`/`RENDER_TIMEOUT` (`tests/unit/print-request.test.ts`, new)
- [ ] T070 [P] Create `apps/frontend/tests/unit/print-request.test.ts`: `errorResponse` status/body/no-store for every `PrintErrorCode`; `runRender` maps `RenderTimeoutError` → 504 and a generic error → 500; `contentDisposition` header is set by `pdfResponse`
- [X] T071 [P] Update `apps/frontend/AGENTS.md` with a "Printing: PDF menu and table stickers (feature 007)" section: the frontend renders documents with `playwright-core` at `/[locale]/print/**` (session-gated, light-only, `force-dynamic`); Route Handlers under `/api/print/**` are the only thing the browser calls; entitlement is `resolveBranding(planOf(account), requested)` server-side; `qr-foreground`/`qr-surface` are the only colours a QR code may use; `MenuSections` is the shared body for guest and print; the four `PDF_*` variables and the Chromium deployment requirement; `?table=N` is carried, never read, by the guest page
- [ ] T072 [P] Extend `apps/frontend/README.md` "Printing" section (from T004) with the deployment note: Node server required, `pnpm exec playwright install chromium --with-deps` in the image or `PDF_CHROMIUM_PATH`, not serverless
- [ ] T073 Run the full gates and fix what fails: `pnpm --filter api lint && pnpm --filter api build && pnpm --filter api test && RESEND_API_KEY= pnpm --filter api test:e2e`; `pnpm --filter frontend lint && pnpm --filter frontend typecheck && pnpm --filter frontend build && pnpm --filter frontend test:unit && pnpm --filter frontend test:stories && pnpm --filter frontend test:e2e`
- [ ] T074 Walk `specs/007-pdf-menu-stickers/quickstart.md` manually end to end (steps 1–6, including a real phone scan of a printed page and the psql plan flip) and record any deviation as a fix task or a quickstart correction

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately
- **Foundational (Phase 2)**: Depends on Setup — BLOCKS all user stories
- **US1 (Phase 3)**: Depends on Foundational
- **US2 (Phase 4)**: Depends on Foundational; reuses `PrintDownloadDialog` from US1 (T041) and the handler plumbing, so start after T041 or build the stickers dialog path in parallel on a branch
- **US3 (Phase 5)**: Depends on Foundational and on T041 (the dialog it adds a switch to); independently testable once either document exists
- **US4 (Phase 6)**: Depends on the two PDF handlers (T040, T053) and the dialog; independently testable
- **Polish (Phase 7)**: Depends on all desired stories

### User Story Dependencies

- **US1 (P1)**: After Foundational. No dependency on other stories.
- **US2 (P1)**: After Foundational. Shares the dialog component with US1 (adds the count field); everything else (QR, sticker components, page, handler) is its own.
- **US3 (P2)**: After Foundational. Adds a switch to the dialog and reads `plan`; the entitlement function already exists from Phase 2.
- **US4 (P3)**: After US1 and US2 handlers exist (the preview endpoints mirror them).

### Within Each User Story

- Components before pages, pages before handlers, handlers before the dialog wiring
- Stories (Storybook) written alongside each component
- e2e written last in the phase and green before the checkpoint

### Parallel Opportunities

- Phase 1: T002, T003, T004 in parallel after T001
- Phase 2: API track (T005–T012) and frontend tracks (types T013–T018; tokens T019–T022; renderer T025–T029; layout/components T030–T033; helpers T034–T036) are independent of one another; within the frontend, T023→T024 is sequential
- Phase 3: T037/T038 with T042/T044; T045 and T046 after wiring
- Phase 4: T047, T048, T049, T050 in parallel; T051 after them; T056, T057, T058 in parallel after T054
- Phase 6: T063 and T064 in parallel
- Phase 7: T068–T072 in parallel; T073 then T074

---

## Parallel Example: User Story 2

```bash
# QR and sticker building blocks together:
Task: "Create lib/pdf/qr.ts + tests/unit/qr.test.ts"                 # T047
Task: "Create lib/pdf/sticker-url.ts"                                 # T048
Task: "Create components/print/QrCode.tsx + stories"                  # T049
Task: "Create components/print/Sticker.tsx"                           # T050

# Then, sequentially:
Task: "Create components/print/StickerSheet.tsx + stories"            # T051
Task: "Create app/[locale]/print/stickers/[menuId]/page.tsx"          # T052
Task: "Create app/api/print/stickers/[menuId]/route.ts"               # T053
Task: "Add the count field to PrintDownloadDialog"                    # T054

# Tests together:
Task: "print.spec.ts: table stickers"                                 # T056
Task: "print.spec.ts: QR decode"                                      # T057
Task: "public-menu.spec.ts: ?table=3 identical"                       # T058
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Phase 1 (Setup) → Phase 2 (Foundational: plan column, tokens, `MenuSections`, renderer, layout, messages, helpers)
2. Phase 3 (US1): print composition, print page, PDF handler, dialog + triggers, e2e
3. **STOP and VALIDATE**: a Free owner downloads a styled A4 menu PDF from the editor; all gates green
4. Demo

### Incremental Delivery

1. Setup + Foundational → foundation ready (API already serving `plan`)
2. US1 → menu PDF (MVP)
3. US2 → table stickers with scannable QR codes
4. US3 → branding switch for paid plans, real plan name in settings
5. US4 → live first-page preview in both dialogs
6. Polish → budgets, docs, resilience, full gates, quickstart walkthrough

### Parallel Team Strategy

1. Everyone: Setup + Foundational (API developer on T005–T012; frontend developers split tokens/CSS, renderer, layout/messages)
2. Then: Developer A → US1; Developer B → US2 components/page/handler (merging the dialog count field after T041 lands); Developer C → US3 switch + subscription page, then US4 preview endpoints
3. Polish together

---

## Notes

- [P] tasks touch different files and have no dependency on an incomplete task
- Every new component ships with a colocated `*.stories.tsx`; the second story pass (slate/dark/de) is what catches hard-coded values
- The renderer must never be imported from a Client Component; if `playwright-core` or `qrcode` shows up in a client bundle, the build should fail loudly (add them to `serverExternalPackages` only; do not create a `"use client"` module that imports `lib/pdf/*`)
- Commit after each task or logical group with `<type>: <summary>` messages
- Stop at any checkpoint to validate the story independently

---

## Implementation status (2026-09-04)

**Built and verified: 49 of 74 tasks.** Every implementation task across Phases 1–6 is
complete — the menu PDF, the table stickers, the branding entitlement and the live preview all
work end to end. The 25 open tasks are the automated-test and documentation layer, deferred at
the user's request ("pro mode we can test later, right now i need qr codes and pdf menus").

### Verified by hand against a running stack

Postgres, the API and a production frontend build, with a real account, a Refined menu of three
sections and fifteen dishes, published:

| Check | Result |
|---|---|
| Menu PDF download | `200`, 216 KB, 3 pages, `attachment; filename="…-jidelni-listek.pdf"` |
| Sticker sheet, 10 stickers | `200`, 197 KB, exactly 3 pages (4 + 4 + 2) |
| Sticker sheet structure | 10 stickers, 10 QR codes, 2 empty cells on the last page |
| QR codes decoded (`jsqr`) | 10 / 10 → `http://localhost:3000/cs/m/<slug>?table=1…10` |
| Table numbering | "Stůl 1" … "Stůl 10" each once; no "Stůl 11" |
| Free plan asking `branding=0` | Restaura line still rendered (FR-017) |
| Pro plan, branding off / on | line absent / present; pages identical otherwise |
| Style switch (Refined → Green Bar) | `data-theme` changes, all 15 dishes present in both |
| Guest page with and without `?table=3` | visible text byte-identical (FR-010, SC-008) |
| Empty menu / draft stickers / no session / unknown menu | `409 EMPTY_MENU` · `409 NOT_PUBLISHED` · `401 UNAUTHENTICATED` · `404 NOT_FOUND` |
| Invalid sticker counts (`0`, `201`) and bad locale | `400 VALIDATION_FAILED` |

Gates: `pnpm --filter api test` (149) and `test:e2e` (162) green; `pnpm --filter frontend
test:unit` (370) green, including the new `qr-foreground`/`qr-surface` contrast pair in all
seven themes; frontend `typecheck` and `build` clean; the design-token and message-catalogue
gates pass; ESLint clean on every file this feature touches.

### Deferred — automated tests and docs

- **Unit** — T006 (API plan literal pin), T016/T017 (plans + print validation), T038/T044/T055/
  T060/T066 (Storybook stories for the print compositions and the dialog), T070 (`print-request`).
- **End-to-end** — T034/T035/T036 (`setPlan`, download and PDF/QR helpers, bulk seeding), T045/
  T046/T056/T057/T058/T062/T067 (`print.spec.ts` and the `?table` case in `public-menu.spec.ts`),
  T068/T069 (budget and concurrency cases).
- **Docs** — T004 and T072 (`README.md` printing section; `.env.example` and `AGENTS.md` are done),
  T073/T074 (full-gate run and the quickstart walkthrough).

The constitutions require these before merge. The behaviour each one would pin has been checked
by hand, as recorded above, but nothing yet stops a regression.

### Known issue, pre-existing and unrelated

`pnpm --filter frontend lint` fails on `components/workspace/ImageCropDialog.tsx:82`
(`react-hooks/set-state-in-effect`), a feature-006 file this work does not touch. Confirmed
present with these changes stashed. Because `lint` chains the three gates with `&&`, the token
and message gates were run directly instead; both pass.
